On activation, I disabled asset auto refresh. To enable auto refresh:
- Edit -> Preferences -> Asset Pipeline -> Auto-Refresh
To refresh assets with the pipeline disabled:
- Assets -> Refresh (Ctrl + R)
